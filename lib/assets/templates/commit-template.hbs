[`{{commit.short}}`](https://github.com/{{owner}}/{{repo}}/commit/{{commit.short}}) {{subject}} - author: {{this.author.name}} - date: {{date}} {{this.date}} {{commit.date}} {{this.commit.date}} - author another way: {{commit.author.name}} {{this.commit.author.name}}
{{#if issues}}
(Issues:{{#each issues}} [`{{text}}`]({{link}}){{/each}})
{{/if}}